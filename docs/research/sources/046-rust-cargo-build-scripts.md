# S046 — Rust Project Developers (n.d.)

## Full reference:

Rust Project Developers (n.d.) ‘Build Scripts’, *The Cargo Book*.

#### https://doc.rust-lang.org/cargo/reference/build-scripts.html

## Relevance:

Supports the requirement to build the Rust application and native CUDA C++ components together through Cargo using a package build script.

## Reliability:

The Cargo Book is the official reference documentation maintained by the Rust project. It is authoritative for Cargo build-script inputs, outputs and linker instructions.

## Tags

`rust`, `cargo`, `build-script`, `native-code`

## Reading notes

- Cargo runs a package's build script before compiling the package.
- Build scripts can emit instructions that affect compilation and linking.

#### Date added

21/07/26
