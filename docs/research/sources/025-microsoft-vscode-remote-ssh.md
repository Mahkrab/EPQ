# S025 — Microsoft (2026)

## Full reference:

Microsoft (2026) ‘Remote Development using SSH’, *Visual Studio Code documentation*.

#### https://code.visualstudio.com/docs/remote/ssh

## Relevance:

Documents the VS Code Remote-SSH workflow used in the project, including named SSH hosts, SSH configuration files and opening a remote development environment through an SSH connection.

## Reliability:

This is the official Visual Studio Code documentation published by Microsoft and is authoritative for the behaviour and configuration of the Remote-SSH extension.

## Tags

`vscode`, `remote-ssh`, `development`, `ssh-config`

## Reading notes

- Remote-SSH connects VS Code to an SSH host for remote editing and terminal access.
- Named hosts can be stored in an SSH configuration file and reused by the extension.

#### Date added

15/07/26
